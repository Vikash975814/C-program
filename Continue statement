#include<stdio.h>
#include<conio.h>
void main()
{
    int i,n;
    printf("enter the number");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        if(i%5==0)
        {
            continue;
        }
        else
        {
            printf("%d\n",i);
        }
        getch();
    }
    
}
