#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{
    int x,y,z;
    float area,s;
   printf("enter the three sides of tringle");
   scanf("%d%d%d",&x,&y,&z);
   s=(x+y+z)/2;
   area=pow(s*(s-x)*(s-y)*(s-z),0.5);
   printf("area is %f",area);
   getch();
   return 0;
}


