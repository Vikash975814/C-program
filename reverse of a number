#include<stdio.h>
       #include<conio.h>
       void main()
{
      int n,r,sum=0;
      printf("Input a number: ");
      scanf("%d",&n);
      for(n;n>0;n=n/10)
    {
         r=n % 10;
         sum=sum*10+r;
    }
       printf("The number in reverse order is : %d \n",sum);
    getch();
}

