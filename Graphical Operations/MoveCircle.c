#include<graphics.h>
#include<conio.h>
Void main()
{
	int gd, gm, i;	
	char ch;

detectgraph (&gd, &gm);
initgraph(&gd, &gm);

circle (200,200, 70);

	ch = getch();
	if(ch == 13)
	
circle (250, 200, 70);

getch();
	}
	







